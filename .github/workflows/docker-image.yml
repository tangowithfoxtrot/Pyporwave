name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: docker login
      env:
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      run:
        docker login -u ${{ secrets.DOCKER_USER }} -p ${{ secrets.DOCKER_PASSWORD }}
    - name: docker build
      run: docker build . --file Dockerfile --tag ${{ secrets.DOCKER_USER }}/pyporwave:latest
    - name: docker push
      run: docker push ${{ secrets.DOCKER_USER }}/pyporwave:latest

    - name: push README to Dockerhub
      uses: christian-korneck/update-container-description-action@v1
      env:
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
        DOCKER_PASS: ${{ secrets.DOCKER_PASSWORD }}
      with:
        destination_container_repo: ${{ secrets.DOCKER_USER }}/pyporwave
        provider: dockerhub
        short_description: 'A Python program to vaporwave-ify any song you want.'
        readme_file: 'README.md'

